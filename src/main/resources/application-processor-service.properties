# Processor Service Configuration (MS3)
spring.application.name=processor-service
server.port=8083

# Database
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}/monetra
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:admin}
spring.datasource.driver-class-name=org.postgresql.Driver

# Flyway - disabled (user-service handles migrations)
spring.flyway.enabled=false

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Security - minimal config for internal service
api.security.token.secret=${JWT_SECRET:12345678}

# Swagger - disabled for processor
springdoc.swagger-ui.enabled=false

# Kafka Consumer
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:29092}
spring.kafka.consumer.group-id=transaction-processor
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=com.finance.api.infra.kafka

# MockAPI for balance checking
bank.api.url=${API_MOCK:http://localhost:3001/balances}

# Service identification
service.name=processor-service
service.description=MS3 - Kafka Consumer, Balance Validation, Transaction Approval
